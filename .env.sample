# Home Inventory App - Environment Variables
# Instructions:
# 1. Copy this file to .env: cp .env.sample .env
# 2. Replace placeholder values with your actual credentials
# 3. Never commit your .env file to version control

# Flask Configuration
FLASK_SECRET_KEY=your_secret_key_here
SECURITY_PASSWORD_SALT=your_password_salt_here

# Mailjet Configuration
MAILJET_API_KEY=your_mailjet_api_key
MAILJET_SECRET_KEY=your_mailjet_secret_key
MAILJET_SENDER_EMAIL=your_sender_email@example.com
MAILJET_SENDER_NAME=Home Inventory App

# Database Configuration (optional, defaults to SQLite)
# DATABASE_URL=postgresql://user:password@localhost/dbname

# ProxyFix Configuration
# Set to 1 to disable ProxyFix (useful for local development without a proxy)
# NOT_BEHIND_PROXY=0

# Testing Configuration
# These are automatically set by the testing framework, don't set manually
# FLASK_TESTING=0
# E2E_TEST=0
